<h2>Your Goods</h2>
<ul><% for item in @cart.line_items %>
  <li><%= item.quantity %> &times; <%= item.product.title %></li>
<% end %>
</ul>

<%= button_to 'Empty cart', @cart, :method => :delete, :data => { :confirm => 'Are you sure?' } %>
